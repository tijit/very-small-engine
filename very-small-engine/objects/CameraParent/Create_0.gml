visible = false;

/// instant: if true dont slowly lerp (eg for first of room)
/// World automatically calls update() and takes the (x,y) of the camera to reposition the screen
update = function(instant=false) {
	x = 0;
	y = 0;
};
